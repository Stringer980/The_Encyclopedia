function titleCase(str) {
  
  str = str.toLowerCase();
  str = str.split(" ");
  let arr =[];
  for (let i = 0; i < str.length; i++){ 
  let a = str[i][0].toUpperCase() + str[i].slice(1) 
      arr.push(a);
    } return arr.join(" "); 
  }
  


console.log(titleCase("I'm a little tea pot"));