 function factorialize(num) {
  if(num < 1) {
  return [];
  } else {
    const countUpArr1 = factorialize(num-1);
countUpArr1.push(num);
console.log(countUpArr1.push(num))
}
}
 /* function factorialize (countUp, num) {
  if ( num <= 0){
    return 1;
    } else {
      //return countUp[num -1] * factorialize (countUp, num)
      }
    }
    console.log() */
  
factorialize(5);
